/**
 * Web Vitals Reporter Component
 *
 * Automatically tracks and reports Web Vitals metrics.
 * Add this component to your root layout to enable monitoring.
 *
 * @example
 * ```tsx
 * // In app/layout.tsx
 * import { WebVitalsReporter } from '@/components/performance/web-vitals-reporter';
 *
 * export default function RootLayout({ children }) {
 *   return (
 *     <html>
 *       <body>
 *         <WebVitalsReporter />
 *         {children}
 *       </body>
 *     </html>
 *   );
 * }
 * ```
 */
export declare function WebVitalsReporter(): null;
//# sourceMappingURL=web-vitals-reporter.d.ts.map